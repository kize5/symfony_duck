<form method="post" action="{{ path('app_duck_delete', {'id': duck.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ duck.id) }}">
    <button class="btn">Delete</button>
</form>
