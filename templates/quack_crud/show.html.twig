{% extends 'base.html.twig' %}

{% block title %}Quack{% endblock %}

{% block body %}
    <h1>Quack</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ quack.id }}</td>
        </tr>
        <tr>
            <th>Content</th>
            <td>{{ quack.content }}</td>
        </tr>
        <tr>
            <th>Created_at</th>
            <td>{{ quack.createdAt| date('d-m-y') }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quack_crud_index') }}">back to list</a>

    <a href="{{ path('app_quack_crud_edit', {'id': quack.id}) }}">edit</a>

    {{ include('quack_crud/_delete_form.html.twig') }}
{% endblock %}
