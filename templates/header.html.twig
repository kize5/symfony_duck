<div style="background-color: #93c5fd ;display: flex; flex-direction: column; align-items: center" class="container_header">
    <div class="logo_header">
        <a href="{{ path('app_home') }}">
<img width="100" height="100" src="https://www.freepnglogos.com/uploads/duck-png/duck-png-transparent-image-pngpix-5.png" alt="logo" class="logo">
        </a>
    </div>
    <div style="display: flex" class="menu_header">

{#<a style="margin: 10px" href="{{ path('app_quack_crud_index') }}">CRUD Quack</a>#}


{% if is_granted('IS_AUTHENTICATED_FULLY') %}

    <a style="margin: 10px" href="{{ path('app_quack_crud_new')}}">QUACK !</a>
    <a style="margin: 10px" href="{{ path('app_duck_show', {'id': app.user.id}) }}">Profile</a>
    <a style="margin: 10px" href="{{ path('app_logout') }}">Logout</a>

{% else %}
    <a style="margin: 10px" href="{{ path('app_login') }}">Login</a>

    <a style="margin: 10px" href="{{ path('app_register') }}">Register</a>
{% endif %}
    </div>
</div>